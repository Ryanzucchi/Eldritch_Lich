# Required in production. Generate with: openssl rand -base64 64
JWT_SECRET=

# Optional: PostgreSQL connection (not yet active, planned for future migration from JSON files)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/eldritch_lich

# Optional: Redis
# REDIS_URL=redis://localhost:6379

# Optional: MinIO / S3
# S3_ENDPOINT=http://localhost:9000
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_BUCKET=eldritch-lich
# Optional local-only Stanza companion. Leave blank to use the conservative in-browser fallback.
NEXT_PUBLIC_LOCAL_LINGUISTICS_URL=http://127.0.0.1:8008
