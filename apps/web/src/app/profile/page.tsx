'use client';
import{FormEvent,useEffect,useState}from'react';import{useApp}from'../../context/AppContext';
export default function ProfilePage(){const{refreshSession}=useApp();const[name,setName]=useState('');const[email,setEmail]=useState('');const[bio,setBio]=useState('');const[message,setMessage]=useState('');useEffect(()=>{fetch('/api/auth/profile').then(r=>r.ok?r.json():null).then(data=>{if(data?.user){setName(data.user.name??'');setEmail(data.user.email??'');setBio(data.user.bio??'')}})},[]);const save=async(e:FormEvent)=>{e.preventDefault();const r=await fetch('/api/auth/update-profile',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,bio})});const d=await r.json();setMessage(r.ok?'Perfil salvo localmente.':d.message??'Não foi possível salvar.');if(r.ok)void refreshSession()};return <main className="page"><header className="page-heading"><div><p className="eyebrow">Conta</p><h1>Perfil</h1><p>Dados usados para identificar suas contribuições no projeto.</p></div></header><form className="surface-card lore-form" onSubmit={save}><label>Nome<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Biografia<textarea value={bio} onChange={e=>setBio(e.target.value)} rows={5} placeholder="Como você escreve ou colabora?"/></label>{message&&<p className="form-error">{message}</p>}<div><button className="primary-button">Salvar perfil</button></div></form></main>}
